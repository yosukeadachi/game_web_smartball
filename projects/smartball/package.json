{
  "name": "smartball",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "deploy": "rm -Rf ./../../docs && mkdir ./../../docs && cp -r ./dist/* ./../../docs/"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  }
}
